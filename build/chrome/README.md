# Chrome target

Output: `dist/chrome`

The Chrome package is the shared Manifest V3 extension. There is no separate detection, conversion, popup, or license implementation.

```bash
npm run build:chrome
```

Load unpacked from `dist/chrome` at `chrome://extensions`.
